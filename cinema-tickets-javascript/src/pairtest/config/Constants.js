// The lowest valid of account id
export const MIN_ACCOUNT_ID = 0;

// The maxium number of total tickets per transaction
export const MAX_TICKETS_PER_TRANSACTION = 20;

// Current business logic states that an infant has to sit on an adults knee
export const MAX_INFANTS_PER_ADULT = 1;

// Stores the cost by ticketType
export const COST_BY_TICKET_TYPE = {
  ADULT: 20,
  CHILD: 10,
  INFANT: 0,
};
